node_modules
dist
test-results
playwright-report
.DS_Store
.pnpm-store
.superpowers/
.e2e-data/
e2e/.platform-data/
tmp-test-list.txt
.worktrees/
server/data/
# Kiriko Rust core build outputs
/core/target/
core/crates/kiriko-node/index.js
core/crates/kiriko-node/index.d.ts
core/crates/kiriko-node/*.node
# Windows leaves this behind when the addon is loaded and cannot be replaced
# in place; `*.node` above does not match the extra suffix.
core/crates/kiriko-node/*.node.inuse
core/crates/kiriko-wasm/pkg/
# Local scratch: Vite's dep scan treats every root HTML file as an entry point,
# so these must stay out of the project root's tracked surface.
/graphify-out/
/penpot design/

# Derived spike scenes (reproducible via kiriko-scene example derive_tiles)
public/spike/

# Gate 7 registration scratch (analysis script, intermediate GeoJSON)
.diag/

# Testing account list: real addresses, kept out of the repository. The shape is
# documented by server/seed-users.example.json.
seed-users.json
